import Fiber from 'fiber';
import playerTemplate from './player.html';

class PlayerComponent extends Fiber.UIComponent.withTemplate(playerTemplate) {

    listen() {

    }
}

export default PlayerComponent;
